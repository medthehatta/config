function _source_if_present {
    [ -f "$1" ] && . "$1"
}


_source_if_present "$HOME/domino/config/.key-exports"
export BROWSER="/usr/bin/google-chrome"
export PATH="${KREW_ROOT:-$HOME/.krew}/bin:$PATH"
